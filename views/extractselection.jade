extends helplayout

block content
  div.jumbotron
    h1=title
  .col-md-6
        h3 Select the extract results you want to see.
        p The left sidebar the page shows the document or aggregation name and extract.
        ul.list-group
          li.list-group-item 
            h2 Namepace Gauges
            p Which of the schema elements and attributes were hit.
            h2 Style Families
            p The styles defined in the document
              em (does not apply to aggregation runs).
            h2 XPath Table
            p A drop-down table view of the xpaths found
            h2 XPath Graph
            p A graph view of the xpaths found
